import { PageHeader } from "@/components/page-header";

/** 模型管理 — operational console (list + control + live logs). Stub; Stage 3. */
export default function ModelsPage() {
  return (
    <>
      <PageHeader title="模型管理" subtitle="模型列表 · 启停控制 · 实时日志" />
      <div className="rounded-lg border border-dashed border-border p-16 text-center text-sm text-muted-foreground">
        建设中
      </div>
    </>
  );
}
