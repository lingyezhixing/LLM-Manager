# 依赖以 pyproject.toml 为唯一来源（核心 6 项 + extras）。
# 本文件仅为兼容 `pip install -r requirements.txt` 习惯保留，等价执行下方命令。
#
# 仅运行（NVIDIA nvidia-smi 默认可用；AMD 780M 监控需 monitoring，系统托盘需 tray）：
-e .[monitoring,tray]
#
# 开发 / 测试（pytest + pytest-asyncio + ruff + pyright）：
#   pip install -e ".[monitoring,tray,dev]"
